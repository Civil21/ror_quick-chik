
<nav class="navbar navbar-default navbar-fixed-top" <%if !@backgroung %> style="position: relative; background-image: url(  /assets/small_index.jpg);" <%end%>>
  <div class="container-fluid">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#myNavbar">
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
     <b> <%=link_to "QuickChik", root_path, class:"navbar-brand"%></b>
    </div>
    <div class="collapse navbar-collapse" id="myNavbar">
      <ul class="nav navbar-nav navbar-right">
        <li><a href="/pages/about">ABOUT</a></li>
        <li class="dropdown">
          <a class="dropdown-toggle" data-toggle="dropdown">CATEGORY
            <span class="caret"></span>
          </a>
          <ul class="dropdown-menu">
            <% Category.all.each do |category|%>
            <li><%=link_to category.name , category_path(category.name)%></li>
            <%end%> 
          </ul>
        </li>
        <%if user_signed_in? %>
          <li>
            <a class="dropdown-toggle" data-toggle="dropdown">
              <span class="glyphicon glyphicon-user"></span>
            </a>
            <ul class="dropdown-menu"> 

              <li>
              <%=link_to "Профіль", userparam_path(current_user.userparam.id)%>
              </li>
              <li> 
                <%= link_to destroy_user_session_path, method: :delete do%><span class="glyphicon glyphicon-log-out"></span><%end%>
              </li>
            </ul>
          </li>
         <%else%>
            <li>
            <%= link_to new_user_session_path do %><span class="glyphicon glyphicon-log-in"></span><%end%>
            </li>
          
        <%end%>
      </ul>
    </div>
  </div>
</nav>

